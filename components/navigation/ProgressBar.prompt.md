Segmented progress meter — the character evolution bar and step counters.

```jsx
<ProgressBar value={2} total={5} />
```

Renders `total` pill segments; the first `value` fill brand-green with a soft glow. Used under the mascot ("인간화 2단계") and for routine step progress.
