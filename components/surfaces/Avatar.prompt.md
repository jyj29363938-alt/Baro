Circular avatar with neutral-gray placeholder.

```jsx
<Avatar src="/uploads/friend.jpg" size={56} />
<Avatar ring size={120} />   {/* profile header */}
```

Falls back to the app's gray placeholder circle when no `src`. Set `ring` for a brand-green highlight ring.
